# New API 二次开发需求文档与开发提示词

版本：V2.0  
基线源码：QuantumNous/new-api  
官方基线 commit：87cc22d7ec4923ad08df826866fb3e51c95ed1d8  
目标仓库：https://github.com/zhangdc1/newapi0604

## 1. 开发目标

本次二次开发只允许完成两项工作：

1. 新增一套分销系统。
2. 修改 DeepSeek 模型 token 统计方式，使其优先使用 DeepSeek 官方返回的 usage。

除以上两项外，现有页面、支付、兑换码、渠道、用户、模型、价格、倍率、权限、Docker 配置、数据库配置均保持原样。

## 2. 分销系统需求

### 2.1 核心规则

奖励触发条件为：被邀请人的账户额度发生正向增加。

触发来源包括：

- 微信/支付宝等在线支付成功后增加额度。
- 兑换码兑换成功后增加额度。
- 管理员后台手动增加额度，是否触发由后台配置控制。
- 未来新增的其他充值方式，只要调用统一额度增加逻辑，也应进入分销检测。

兑换码在链动小铺等外部平台购买时，New API 无法获得外部购买回调，因此不以外部购买行为作为奖励依据。用户回到 New API 成功兑换兑换码并增加额度时，才触发分销奖励。

### 2.2 邀请关系

- 邀请人生成邀请链接，链接携带 `aff` 或 `dist_id` 参数。
- 新用户通过邀请链接注册后绑定邀请人。
- 一个用户只能绑定一个邀请人。
- 不能自邀。
- 已绑定关系不可被后续邀请链接覆盖。
- 被禁用或不存在的用户不能作为有效邀请人。

### 2.3 奖励配置

管理员可配置：

- 是否启用分销系统，默认关闭。
- 奖励模式：按比例或固定额度。
- 奖励比例，默认 10%。
- 固定奖励额度。
- 每个被邀请人可触发奖励次数，0 表示无限制。
- 普通用户与代理分开配置奖励规则。
- 普通用户默认仅被邀请人的前 5 次充值可获得奖励，后台可改为 6 次、10 次或 0 表示无限制。
- 代理默认不限制奖励次数，并使用独立的代理奖励比例或固定额度。
- 冻结期天数，默认 0。
- 最低转入门槛。
- 管理员后台充值是否触发奖励，默认关闭。
- 兑换码临时归因是否启用，默认开启。

配置变更只影响新产生的奖励，不回溯修改历史奖励。

### 2.4 奖励结算

- 奖励产生后写入奖励记录。
- 未过冻结期的奖励不可转入账户额度。
- 过冻结期后，用户可在分销中心点击“奖励转余额”。
- 奖励只能转入平台账户额度，不能提现吗或转出现金。
- 奖励转余额自身不得再次触发分销奖励。

### 2.5 后台管理

后台应提供：

- 分销设置。
- 代理管理，可将普通用户设为代理或取消代理。
- 奖励记录查询。
- 邀请关系查询。
- 奖励转入记录查询。

所有记录应可按邀请人、被邀请人、状态、来源、时间筛选。

## 3. DeepSeek Token 统计

DeepSeek 模型 token 统计规则：

1. DeepSeek 官方响应中包含 `usage` 时，必须以官方 `usage` 为最终统计依据。
2. 流式响应需要读取最终 usage。
3. 只有上游没有返回 usage 时，才允许使用 DeepSeek 官方 tokenizer 或兼容估算作为兜底。
4. 不修改其他模型的 token 统计和计费逻辑。
5. 不修改现有价格、倍率、渠道、分组和额度扣费规则。

## 4. 生产数据保护边界

正式环境为 PostgreSQL + Redis + Docker。

必须保证：

- 不删除 PostgreSQL 数据库。
- 不删除 Docker volume。
- 不执行 `docker compose down -v`。
- 不运行空库初始化覆盖正式数据。
- 不清空用户、渠道、令牌、余额、配置、兑换码、日志等表。
- 数据库迁移只允许新增表、新增字段、新增索引。
- 新增配置只在不存在时插入，不能覆盖现有配置。

## 5. 开发提示词

你是一名资深全栈工程师，请基于官方 QuantumNous/new-api 最新源码开发。开发前必须阅读现有用户、额度、充值、兑换码、支付回调、设置项、路由、前端页面、DeepSeek 适配器和 token 统计逻辑。

严格限制改动范围：

- 只新增分销系统。
- 只修正 DeepSeek token 统计。
- 分销前端只允许在经典前端中新增必要页面和入口，不得改变原经典前端字体、主题、全局样式和其它页面。
- 其他功能原汁原味。

禁止：

- 重构无关模块。
- 改现有接口语义。
- 改非 DeepSeek 模型 token 统计。
- 改支付链路。
- 改兑换码购买方式。
- 改 Docker volume 或生产数据库配置。
- 批量格式化无关文件。

交付时必须提供：

- 修改文件清单。
- 数据库迁移说明。
- 新增接口说明。
- 测试结果。
- PostgreSQL + Redis + Docker 无损替换部署教程。
- 回滚方案。
